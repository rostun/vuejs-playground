.Dashboard
   display flex
   flex-direction column
   align-items center
   h1
      color red

   .search_results
      .pet_information
         display grid
         grid-template-columns repeat(3, 1fr)
         grid-gap 10px
         grid-auto-rows minmax(25px, auto)
