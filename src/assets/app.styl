.full-width
   width 100%

.center-content
   display flex
   justify-content center
   align-items center
   flex-direction column

.title
   padding 25px 0
   font-size 1.5em
   font-weight bold

.font-style
   font-family monospace